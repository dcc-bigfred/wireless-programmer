[package]
name = "wp-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "DeviceDriver trait, capabilities and typed errors for wireless-programmer"

[lib]
name = "wp_core"
path = "src/lib.rs"

[dependencies]
wp-proto = { path = "../wp-proto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true
